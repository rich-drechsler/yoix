#
# This file is included by Makefiles that build and install your server.
# The top-level Makefile (i.e., ../Makefile) is the exception because it
# defines its own rules for targets, like "all" and "install", that are
# also defined in rules.mk.
# 

include $(ROOT)/make/variables.mk
include $(ROOT)/make/rules.mk

